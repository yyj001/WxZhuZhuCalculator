<!--index.wxml-->
<view class="vertical_layout">
	<!-- <view class="userinfo">
    <button wx:if="{{!hasUserInfo && canIUse}}" open-type="getUserInfo" bindgetuserinfo="getUserInfo"> 获取头像昵称 </button>
    <block wx:else>
      <image bindtap="bindViewTap" class="userinfo-avatar" src="{{userInfo.avatarUrl}}" mode="cover"></image>
      <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </block>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view> -->
	<view class="tips">
		质量 (mg) = 浓度 (mM) x 体积 (mL) x 分子量 (g/mol)
	</view>
	<!-- 质量 -->
	<view class="horizontal_layout">
		<view>
			<image src="../images/quality.png" class="text_icon"></image>
		</view>
		<text class="text-label"> 质量:</text>
		<input class="edittext" type="number" value="{{qualityValue}}" bindinput="qualityInput">
		</input>
		<view>
			<image src="../images/down.png" class="picker_icon"></image>
		</view>
		<view>
			<picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
				<view>
					{{array[index]}}
				</view>
			</picker>
		</view>
	</view>
	
	<!-- 浓度 -->
	<view class="horizontal_layout">
		<view>
			<image src="../images/concentration.png" class="text_icon"></image>
		</view>
		<text class="text-label"> 浓度:</text>
		<input class="edittext" type="number"></input>
		<view>
			<image src="../images/down.png" class="picker_icon"></image>
		</view>
		<view>
			<picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
				<view>
					{{array[index]}}
				</view>
			</picker>
		</view>
	</view>

<button bindtap="calculate">计算</button>


</view>